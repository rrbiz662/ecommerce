pipeline {
    agent {
        docker {
            image 'maven'
            args '--volume /root/.m2:/root/.m2'
        }
    }

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                /*
                sh 'mvn -DskipTests clean package'
                */
            }
        }
        stage('Test') {
            steps {
                echo 'Testing'
            }
            /*
            post {
                always {
                    junit 'target/surefire-reports/*.xml'
                }
            }*/
        }
        stage('Deploy') {
            steps {
                sh 'docker build --tag ecommerce-app .'
                sh 'docker run --publish 8080:8080 ecommerce-app'
                /*
                echo 'Deploying'
                sh "chmod +x -R ${env.WORKSPACE}"
                sh './jenkins/scripts/deliver.sh'
                */
            }
        }
    }
}